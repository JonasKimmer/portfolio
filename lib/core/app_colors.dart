import 'package:flutter/material.dart';

class AppColors {
  // Primärfarben (Beispiel: Blau)
  static const primary = Color(0xFF1E88E5);
  static const primaryLight = Color(0xFF6AB7FF);
  static const primaryDark = Color(0xFF005CB2);

  // Sekundärfarben (Beispiel: Amber)
  static const accent = Color(0xFFFFC107);
  static const accentLight = Color(0xFFFFD54F);
  static const accentDark = Color(0xFFFFA000);

  // Hintergrund- und Oberflächenfarben
  static const background = Color(0xFFF5F5F5);
  static const surface = Colors.white;

  // Text- und Iconfarben auf den jeweiligen Flächen
  static const onPrimary = Colors.white;
  static const onSecondary = Colors.black;
  static const onBackground = Color(0xFF212121);
  static const onSurface = Color(0xFF212121);

  // Fehlerfarben
  static const error = Color(0xFFD32F2F);
  static const onError = Colors.white;
}
